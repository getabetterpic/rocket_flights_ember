<select onchange={{action 'rocketDidChange' value='target.value'}} id="rocket-select">
  {{#each rockets as |rocket|}}
    <option value={{rocket.id}}
            selected={{eq rocket.id selectedRocket.id}}>
      {{rocket.name}}
    </option>
  {{/each}}
</select>
